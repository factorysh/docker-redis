---

version: '3'

services:

    server:
        image: bearstech/redis:${REDIS_VERSION}

    client:
        image: bearstech/redis:${REDIS_VERSION}
        volumes:
            - ./bin/goss:/usr/local/bin/goss
            - ./compose/redis.yaml:/tests/redis.yaml
        working_dir: /tests
        links:
            - server
        command: goss -g redis.yaml validate
